# aliases: rag

```bash
@rag \
	build_corpus \
	[download,upload] \
	[.|<crawl-object-name>] \
	[-|<corpus-object-name>]
 . build rag.
@rag \
	query \
	[download] \
	[.|<corpus-object-name>] \
	<query> \
	[-|<query-object-name>]
 . query <query> on <corpus-object-name> -> <query-object-name>.
```
